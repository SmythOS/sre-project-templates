// Window configuration
export const WINDOW_CONFIG = {
  // Set to false to use native window frame (with title bar, buttons, etc.)
  // Set to true for frameless, transparent window with custom controls
  useFramelessWindow: true,

  // Window dimensions
  width: 900,
  height: 700,

  // Transparency settings (only works when useFramelessWindow is true)
  transparent: true,
};
